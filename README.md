_Too lazy to make this, but here I am anyways._

## holyRitaDatasetGen.py is the generator (final result being **dataset_rita_instruct.jsonl**), can be left untouched just fine

## instructs.py has the prompts (don't need to be pro coder) and it might or might not need work, depends on how much you like the dataset
(which... I have no clue, I think I like it? I made emote usage changes to prompts in middle of dataset generation ~line 3750, but heavier changes on dataset input key content earlier (like since ~line 300 (idr anymore but imma make a new dataset anyway))
